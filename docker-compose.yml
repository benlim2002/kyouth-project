services:
  backend:
    build: ./backend
    container_name: week4_backend
    ports:
      - "${PORT}:${PORT}"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - PORT=${PORT}
    networks:
      - week4_network

  frontend:
    build: ./frontend
    container_name: week4_frontend
    ports:
      - "8000:8000"
    environment:
      - BACKEND_URL=${BACKEND_URL}
    depends_on:
      - backend
    networks:
      - week4_network

networks:
  week4_network:
    driver: bridge